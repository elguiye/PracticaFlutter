import 'package:flutter/material.dart';
import 'package:flutter_contador/src/pages/home-page.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
